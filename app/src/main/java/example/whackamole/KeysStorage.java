package example.whackamole;

/**
 * Stores bundle's and preferences' keys.
 *
 * @author Vladislav
 * @version 1.0
 * @since 1.0
 */
final class KeysStorage {
    /**
     * A user's high score's key.
     */
    static final String HIGH_SCORE = "HIGH_SCORE";
    /**
     * A user's current score's bundle key.
     */
    static final String MOLES = "MOLES";
    /**
     * An application preferences' file name.
     */
    static final String APP_PREFERENCES = "appPreferences";
    /**
     * A replay status preferences key.
     */
    static final String REPLAY = "replay";
}
